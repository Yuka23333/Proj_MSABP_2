# 结果

- `raw/`：由仿真或测量直接导出的数据，不进行人工修改。
- `processed/`：由代码生成的清洗表、汇总指标、Pareto 前沿和统计结果。
- `figures/`：经过筛选、可直接用于汇报或论文的图。
- `reports/`：实验摘要、对比表和阶段性结论。

建议让 `raw/` 与 `processed/` 按同一个 `experiment_id` 建立子目录，以便一一对应。原始结果和中间结果默认不纳入 Git；最终图表与报告可以纳入版本管理。
